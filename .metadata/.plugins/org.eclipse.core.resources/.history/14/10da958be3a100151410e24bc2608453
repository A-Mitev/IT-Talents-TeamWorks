package horse_move;

import java.util.Scanner;

public class Horse_Move {
	
	static Scanner sc = new Scanner(System.in);
	
	static int firstRow;
	static int firstCol;
	static int row; //zadavame granici na duskata po
	static int col; //koqto shte se dviji konq
	static char[][] board;
	static char horseFirstPosition = '*';
	static int[][] horseMovse = {{1,-2}, {2,-1}, {2,1}, {1,2}, {-1,2}, {-2,1}, {-2,-1}, {-1,-2}};
	
	public static void main(String[] args) {
		
		do{
			row = sc.nextInt();
			if(row == 2){
				do{
					col = sc.nextInt();
				}while(col < row + 1);
			}else{
				do{
					col = sc.nextInt();
				}while(col < 2);
			}
		}while(row < 2);
		
		
		
		board = new char[row][col];
		
		for(int i = 0; i < row; i++){
			for(int j = 0; j < col; j++){
				board[i][j] = ' ';
			}
		}
		
		printBoard();
		
		firstRow = sc.nextInt();
		firstCol = sc.nextInt();
		
		board[firstRow][firstCol] = horseFirstPosition;
		
		horseMove(firstRow, firstCol);
		
		printBoard();
		
	}
	
	static void printBoard() {
		for (int i = 0; i < row; i++) {
			for (int j = 0; j < col; j++) {
				if (j == 0) {
					System.out.print("| ");
				}
				System.out.print(board[i][j] + " | ");
			}
			System.out.println();
		}
		
	}
	
	static void horseMove(int n, int m){
		
		n = n + 1;
		m = m + 2;
		board[n][m] = horseFirstPosition;
		
		
	}
	
}